<Page x:Class="OpticianMgr.Wpf.CustomerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OpticianMgr.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="CustomerPage"
      DataContext="{Binding CustomerViewModel, Source={StaticResource Locator}}">

    <DockPanel>
        <Label DockPanel.Dock="Top" Margin="10,10,0,0" FontSize="18" FontStyle="Italic" Background="{x:Null}" FontWeight="Bold">Kunden</Label>
        <Grid DockPanel.Dock="Top">
            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                <Label Margin="10,0,0,0">Filtern nach:</Label>
                <ComboBox x:Name="PropertyComboBox" ItemsSource="{Binding PropertiesList}" SelectedValue="{Binding FilterProperty}" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBox Width="100" Margin="10,0,0,0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center" >
                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding FilterSuppliers}" Key="Return"></KeyBinding>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Margin="9,0,0,0" Command="{Binding OpenCustomer}" Width="40">Filtern</Button>
                <Button Margin="9,0,0,0" Command="{Binding DeleteFilter}" Width="105">Filter zurücksetzen</Button>
            </StackPanel>
            <Button Margin="9,0,10,0" Command="{Binding AddCustomer}" HorizontalAlignment="Right" Width="155">Neuen Kunden hinzufügen</Button>
        </Grid>

        <ListView ItemsSource="{Binding Customers, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top" SelectedItem="{Binding Selected}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Header="Id" />
                    <GridViewColumn DisplayMemberBinding="{Binding Title, UpdateSourceTrigger=PropertyChanged}"  Header="Titel"/>
                    <GridViewColumn DisplayMemberBinding="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"  Header="Vorname"/>
                    <GridViewColumn DisplayMemberBinding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"  Header="Nachname"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Street, UpdateSourceTrigger=PropertyChanged}"  Header="Straße"/>
                    <GridViewColumn DisplayMemberBinding="{Binding HouseNumber, UpdateSourceTrigger=PropertyChanged}" Header="Hausnummer"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Town.ZipCode, UpdateSourceTrigger=PropertyChanged}" Header="PLZ"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Town.TownName, UpdateSourceTrigger=PropertyChanged}"  Header="Ort"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Telephone1, UpdateSourceTrigger=PropertyChanged}"  Header="Telefon1"/>
                </GridView>
            </ListView.View>
        </ListView>
        <!--<DataGrid CanUserDeleteRows="True" SelectionMode="Extended" CanUserReorderColumns="true" ItemsSource="{Binding CustomerList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top" Background="{x:Null}" Margin="10,13,10,9" VerticalAlignment="Top" AutoGenerateColumns="False" CurrentCell="{Binding SelectedCell, Mode=TwoWay}" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Header="Id" Width="0.5*" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="*" Header="Titel"/>
                <DataGridTextColumn Binding="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Vorname"/>
                <DataGridTextColumn Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Nachname"/>
                <DataGridTextColumn Binding="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Straße"/>
                <DataGridTextColumn Binding="{Binding HouseNumber, UpdateSourceTrigger=PropertyChanged}" Width="0.5*" Header="Hausnummer"/>
                <DataGridTextColumn Binding="{Binding Town.ZipCode, UpdateSourceTrigger=PropertyChanged}" Width="*" Header="PLZ"/>
                <DataGridTextColumn Binding="{Binding Town.TownName, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Ort"/>
                <DataGridTextColumn Binding="{Binding Telephone1, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Telefon1"/>
                <DataGridTextColumn Binding="{Binding Telephone2, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Telefon2"/>
                <DataGridTextColumn Binding="{Binding Insurance, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Versicherung"/>
                <DataGridTextColumn Binding="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged, StringFormat=d}" Width="1.5*" Header="Geburtstag"/>
                <DataGridTextColumn Binding="{Binding InsurancePolicyNumber, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Versicherungnummer"/>
                <DataGridTextColumn Binding="{Binding Country.Name, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Land"/>
                <DataGridTextColumn Binding="{Binding Job, UpdateSourceTrigger=PropertyChanged}" Width="*" Header="Job"/>
                <DataGridTextColumn Binding="{Binding Hobbies, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Hobbies"/>
                <DataGridTextColumn Binding="{Binding Others1, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Sonstiges1"/>
                <DataGridTextColumn Binding="{Binding Others2, UpdateSourceTrigger=PropertyChanged}" Width="2*" Header="Sonstiges2"/>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <KeyBinding Command="{Binding EditSupplier}" Key="Return"></KeyBinding>
            </DataGrid.InputBindings>
        </DataGrid>-->
    </DockPanel>
</Page>
